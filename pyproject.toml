[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ct"
version = "0.1.0"
description = "CT project"
requires-python = ">=3.10"
dependencies = [
	"numpy",
	"pandas",
	"openpyxl",
	"torch",
	"scikit-learn",
	"joblib",
]
